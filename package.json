{
  "name": "cypress-cucumber-plugin-before-spec-test",
  "version": "1.0.0",
  "description": "testing before:spec event handler of the cypress-cucumber-preprocessor plugin",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^17.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "@esbuild-plugins/node-modules-polyfill": "^0.2.2",
    "cypress": "^12.17.3",
    "esbuild": "^0.18.19"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/e2e/*.{js,ts}"
    ],
    "messages": {
      "enabled": true
    }
  }
}
